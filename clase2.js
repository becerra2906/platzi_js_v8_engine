// que es JS Engine

// "el motor de JS" (siempre corre en el navegador)

// tengo una lista de tareas que tengo que programar, 
//y que van a ser las que van a tener que ejecutarse a travez del navegador en un orden

//esta lista de tareas están programadas en js

// el javascript engine es el interprete del codigo de js que llega al navegador para que el computador entienda lo que tiene que ejcutar (que viene en machine code);

// (este proceso se llama just in time compiler (jit compiler));

